module.exports = {
    recaptcha: {
        client_key: process.env.RECAPTION_CLIENT_KEY,
        secret_key: process.env.RECAPTION_SECRET_KEY,
        options: {
            hl: 'fa'
        }

    }
}